# Get a free API key from https://openweathermap.org/api
OPENWEATHER_API_KEY=your_api_key_here

# Comma-separated origin(s) allowed to call the API. Use * for local dev only.
CORS_ORIGINS=http://localhost:5173
